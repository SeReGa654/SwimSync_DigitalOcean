global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: swimsync-backend
    metrics_path: /api/health/prometheus
    static_configs:
      - targets:
          - host.docker.internal:3001
