# Функціонал: Три способи формування фінішних протоколів

## Опис

Додано функціонал вибору метода формування фінішних протоколів результатів змагань. Тепер можна вибрати один з трьох варіантів розподілу учасників по вік. категоріям при експорті протоколів.

## Три варіанти формування

### 1. **SEPARATE** (Окремо по вікових категоріях) — Типова поведінка
- Кожна вікова категорія вводиться окремою таблицею в протоколі
- Місця розраховуються в межах кожної категорії незалежно
- Усі спортсмени из однієї категорії конкурують між собою за місця в цій категорії

**Приклад:**
```
┌─ Таблиця 1: Кадети (2012-2013)
│  ┌─ Місце 1: Петро Іванов
│  ├─ Місце 2: Марія Сидоренко
│  └─ Місце 3: Ігор Коваль
└─ Таблиця 2: Абсолют (2008-2011)
   ┌─ Місце 1: Анна Шевченко
   ├─ Місце 2: Сергій Бондар
   └─ Місце 3: Василина Петрова
```

### 2. **COMBINED** (Об'єднаний рейтинг)
- Всі вікові категорії змішуються в один рейтинг
- Місця розраховуються по часам всіх спортсменів незалежно від вік. категорії
- Справедливий розподіл: швидший час = краще місце, вік. група не важлива

**Приклад:**
```
┌─ Таблиця 1: Об'єднаний рейтинг
│  ┌─ Місце 1: Ігор Коваль (Кадет) — 52.10
│  ├─ Місце 2: Петро Іванов (Кадет) — 52.45
│  ├─ Місце 3: Анна Шевченко (Абсолют) — 51.90
│  ├─ Місце 4: Марія Сидоренко (Кадет) — 53.10
│  └─ Місце 5: Сергій Бондар (Абсолют) — 53.50
```

### 3. **MIXED** (Змішаний варіант)
- Одна вікова категорія показується окремою таблицею (основна група)
- Решта категорій об'єднуються в окремий об'єднаний рейтинг
- Ідеально для виділення однієї категорії, наприклад, Кадетів

**Приклад:**
```
┌─ Таблиця 1: Кадети (основна група)
│  ┌─ Місце 1: Ігор Коваль — 52.10
│  ├─ Місце 2: Петро Іванов — 52.45
│  └─ Місце 3: Марія Сидоренко — 53.10
└─ Таблиця 2: Об'єднаний рейтинг (Абсолют)
   ┌─ Місце 1: Анна Шевченко — 51.90
   └─ Місце 2: Сергій Бондар — 53.50
```

## Як користуватися

### Налаштування формату

1. Перейди на сторінку змагання (`/competitions/[id]`)
2. Відкрий вкладку **Settings** (Налаштування)
3. Прокрути вниз до секції **"Формат фінішних протоколів"**
4. Вибери один з трьох варіантів:
   - **Окремо по вікових категоріях** (Режим SEPARATE)
   - **Об'єднаний рейтинг** (Режим COMBINED)
   - **Змішаний** (Режим MIXED)

### Для режиму MIXED

Якщо вибраний режим **"Змішаний"**, необхідно:

1. Вибрати **"Основна вікова група"** — категорія, які буде показана окремою таблицею
2. Вибрати **"Групи для об'єднаного рейтингу"** — всі інші категорії, які об'єднаються (можна вибрати кілька)

Приклад:
- Основна: **Кадети (2012-2013)**
- Об'єднані: **Абсолют (2008-2011)**, **Молодь (2014-2015)**

3. Натисни **"Зберегти налаштування протокол"**

### Експорт протоколів

Після налаштування, при експорті фінішних протоколів:
- DOCX файли буде згенеровані відповідно до обраного формату
- XLSX (Excel) також буде дотримуватись обраного формату
- Результати в системі залишаться незмінними (місця рахуються за вік. категоріями)

## Технічні деталі

### Backend API

**Endpoint:** `PATCH /api/competitions/:id/result-protocol-config`

**Тіло запиту (SEPARATE):**
```json
{
  "format": "SEPARATE"
}
```

**Тіло запиту (COMBINED):**
```json
{
  "format": "COMBINED"
}
```

**Тіло запиту (MIXED):**
```json
{
  "format": "MIXED",
  "mixedFormatPrimaryAgeGroupId": 1,
  "mixedFormatSecondaryAgeGroupIds": [2, 3]
}
```

### Prisma Schema

Додано три нові поля до моделі `Competition`:
- `resultProtocolFormat`: enum (`SEPARATE` | `COMBINED` | `MIXED`) — вибраний формат
- `mixedFormatPrimaryAgeGroupId`: nullable Int — ID основної групи для MIXED режиму
- `mixedFormatSecondaryAgeGroupIds`: nullable String — JSON масив ID груп для об'єднання

### Логіка экспорту

- **SEPARATE**: Оригінальна логіка — групування за ageGroupId з сортуванням по місцях
- **COMBINED**: Об'єднання всіх груп, перерахунок місць по часам (OK → PK → інші)
- **MIXED**: Первинна група окремо + об'єднана вторинних з перерахунком місць

## Важливо

⚠️ **Примітка:** Налаштування формату впливає тільки на **експорт протоколів** (DOCX/XLSX).  
Внутрішні результати в системі завжди рахуються по вік. категоріях окремо.

## Файли, які були змінені

### Backend
- `backend/prisma/schema.prisma` — розширення моделі Competition
- `backend/src/competitions/competitions.controller.ts` — новий endpoint
- `backend/src/competitions/competitions.service.ts` — логіка налаштувань
- `backend/src/competitions/dto/result-protocol-config.dto.ts` — новий DTO
- `backend/src/export/export.controller.ts` — логіка експорту з трьома форматами

### Frontend
- `frontend/src/types/index.ts` — розширення типу Competition
- `frontend/src/lib/api.ts` — новий API метод `setResultProtocolConfig`
- `frontend/src/app/competitions/[id]/tabs/SettingsTab.tsx` — UI форма

## Тестування

Всі компоненти успішно скомпільовані та перевірені:
- ✅ Backend типи (тільки frontend lint)
- ✅ Frontend типи
- ✅ API контракти
- ✅ Build (backend + frontend)

## Можливі розширення в майбутньому

1. Збереження налаштувань для експорту (приватні/публічні налаштування)
2. Додавання дефолтних налаштувань на рівні користувача
3. Більше гнучкості в розділенні категорій (наприклад, за регіонами)
